/**
 * 布局接口
 *
 * @interface Layout
 */

/**
 * 重排
 * 在实现时，通过计算栈内每个元素的位置来重排，要注意 absolutePosition 的处理
 * @function
 * @name Layout#reflow
 * @param {Group|JFlow} group   - 栈对象
 */

/**
 * 对象布局位置检查
 *
 * @function
 * @name Layout#staticCheck
 * @param {Node} instance   - 目标节点对象
 * @param {JFlow} jflow     - JFlow对象
 * @return {Boolean}
 */